---
name: test-network-org${ORG}
client:
  organization: Org${ORG}
  connection:
    timeout:
      peer:
        endorser: '300'
organizations:
  Org${ORG}:
    mspid: Org${ORG}MSP
    peers:
    - peer0.org${ORG}.raft
    certificateAuthorities:
    - ca.org${ORG}.raft
peers:
  peer0.org${ORG}.raft:
    url: grpcs://localhost:${P0PORT}
    tlsCACerts:
      pem: |
          ${PEERPEM}
    grpcOptions:
      ssl-target-name-override: peer0.org${ORG}.raft
      hostnameOverride: peer0.org${ORG}.raft
certificateAuthorities:
  ca.org${ORG}.raft:
    url: https://localhost:${CAPORT}
    caName: ca-org${ORG}
    tlsCACerts:
      pem: 
        - |
          ${CAPEM}
    httpOptions:
      verify: false

